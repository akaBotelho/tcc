# =============================================================================
# IoT Security Testing Toolkit - Configurações
# =============================================================================
# Copie este arquivo para .env e configure suas credenciais
# cp .env.example .env

# -----------------------------------------------------------------------------
# OWASP ZAP
# -----------------------------------------------------------------------------
ZAP_API_KEY=sua_chave_api_aqui
ZAP_HOST=127.0.0.1
ZAP_PORT=8090

# -----------------------------------------------------------------------------
# NVD (National Vulnerability Database) - Consulta CVE
# -----------------------------------------------------------------------------
# Obtenha sua chave em: https://nvd.nist.gov/developers/request-an-api-key
NVD_API_KEY=sua_chave_nvd_aqui

# -----------------------------------------------------------------------------
# Captura de Tráfego
# -----------------------------------------------------------------------------
DEFAULT_INTERFACE=eth0
CAPTURE_TIMEOUT=600
# Se definido, usa este PCAP ao invés de captura em tempo real
PCAP_FILE=

# -----------------------------------------------------------------------------
# Wordlists (SecLists)
# -----------------------------------------------------------------------------
# Caminho para o repositório SecLists
# git clone https://github.com/danielmiessler/SecLists.git
WORDLIST_PATH=/usr/share/seclists
WORDLIST_PASSWORDS=Passwords/Common-Credentials/10-million-password-list-top-10000.txt
WORDLIST_USERS=Usernames/top-usernames-shortlist.txt
WORDLIST_DEFAULT_CREDS=Passwords/Default-Credentials/default-passwords.csv

# -----------------------------------------------------------------------------
# Rede
# -----------------------------------------------------------------------------
DEFAULT_SUBNET=192.168.0.0/24

# -----------------------------------------------------------------------------
# Firmware
# -----------------------------------------------------------------------------
# Caminho para arquivo de firmware (usado se não detectado na análise de tráfego)
FIRMWARE_PATH=

# -----------------------------------------------------------------------------
# Output
# -----------------------------------------------------------------------------
OUTPUT_DIR=./output
